# ========================================
# BestDoctors Panel - Production Environment
# ========================================
# 
# IMPORTANT: 
# 1. Copy this file to .env.production
# 2. Fill in all values with production credentials
# 3. NEVER commit .env.production to git
# 4. Use strong, randomly generated passwords
#

# ----- Domain & SSL Configuration -----
# Your production domain name (e.g., bestdoctors.com)
TRAEFIK_DOMAIN=example.com

# ----- SuperAdmin Credentials (Admin Panel) -----
# Generate with: openssl rand -base64 32
SUPERADMIN_USERNAME=admin
SUPERADMIN_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# ----- Redis (Session Store) -----
# Generate with: openssl rand -base64 32
REDIS_PASSWORD=CHANGE_ME_REDIS_PASSWORD_HERE

# ----- Database (Production PostgreSQL) -----
# Use managed database service or dedicated server
PG_HOST=your-db-host.rds.amazonaws.com
PG_PORT=5432
PG_USER=postgres
PG_PASSWORD=CHANGE_ME_DB_PASSWORD_HERE
PG_DATABASE=bestdoctors_prod
PG_SSLMODE=require

# ----- Supabase API (Optional) -----
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key

# ----- Twilio (WhatsApp Messaging) -----
TWILIO_URL=https://api.twilio.com/2010-04-01/Accounts/YOUR_ACCOUNT_SID/Messages.json
TWILIO_ACCOUNT_SID=YOUR_TWILIO_ACCOUNT_SID
TWILIO_AUTH_TOKEN=YOUR_TWILIO_AUTH_TOKEN

# ----- Monitoring & Alerting (Optional) -----
# ENABLE_PROMETHEUS=true
# ALERT_EMAIL=alerts@example.com
# SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ----- Backup Configuration (Optional) -----
# AWS_ACCESS_KEY_ID=YOUR_AWS_KEY
# AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET
# AWS_S3_BUCKET=bestdoctors-backups
# AWS_REGION=us-east-1
# BACKUP_RETENTION_DAYS=30
